class AppConfig {
  AppConfig._();

  /// Set to your deployed bridge server URL for production,
  /// or keep as localhost for development.
  static const String bridgeUrl = 'http://localhost:3000';
}
